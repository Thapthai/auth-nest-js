# Database Configuration (External Database)
DATABASE_URL='mysql://username:password@ip:port/database'

# Application Configuration
PORT=3000
DEV_PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET="your_jwt_secret"
JWT_EXPIRES_IN=1d

# Email Configuration (for password reset, verification, etc.)
EMAIL_HOST="sandbox.smtp.mailtrap.io"
EMAIL_PORT=2525
EMAIL_USER="3206898bc00c3f"
EMAIL_PASS="d42570b07251b1"

# CORS Configuration
CORS_ORIGIN=http://localhost:3005,http://localhost:3000,http://localhost:3001

# Two-Factor Authentication
TWO_FACTOR_APP_NAME=AuthNestJS

# Security Notes:
# - Change JWT_SECRET to a strong random string in production
# - Use App Password for Gmail (not your regular password)
# - Restrict CORS_ORIGIN in production to specific domains
# - Never commit .env file to version control 